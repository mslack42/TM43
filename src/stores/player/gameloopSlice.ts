import { createSlice } from '@reduxjs/toolkit'

export const gameloopSlice = createSlice({
  name: 'gameloop',
  // https://redux-toolkit.js.org/usage/nextjs#loading-initial-data
  initialState: {},
  // Steal these from GameplayStateReducer
  reducers: {},
})
